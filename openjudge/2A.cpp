#include <stdio.h>
#include <iostream>
#include <vector>
#include <string>
#include <queue>

using namespace std;

int main(){
    int m, n;
    cin >> m >> n;
    while(m != 0){
        vector<string> map;
        string t;
        for(int i = 0; i < m; i++){
            cin >> t;
            map.push_back(t);
        }
        
    }
}